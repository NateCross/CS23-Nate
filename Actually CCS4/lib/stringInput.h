#ifndef __STRING_INPUT_
#define __STRING_INPUT_

// This function takes in a string input, snips the \n,
// and flushes the input in a cleaner way than fflush(stdin)
void stringInput(char [], int);

#endif
